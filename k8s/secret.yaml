apiVersion: v1
kind: Secret
metadata:
  name: demo-app-secret
  namespace: default  # Add your namespace here (default is used in this case)
type: Opaque
data:
  SPRING_DATASOURCE_PASSWORD: cGFzc3dvcmQ=  # Base64 encoded password, e.g., "password"
